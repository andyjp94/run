{
	"commands": [{
		"command": "local",
		"value": "echo \"global: Setting env in command ${CUSTOM_ENV}\"",
		"env": [{
			"name": "CUSTOM_ENV",
			"value": "works"
		}]
    },
     {
        "command": "cli",
		"value": "echo \"global: Setting env in command ${CUSTOM_ENV}\""
	 },
	 {
        "command": "multiple_cli",
		"value": "echo \"global: Setting env in command ${CUSTOM_ENV}, ${OTHER_CUSTOM_ENV}\""
     },
     {
        "command": "global",
		"value": "echo \"global: Setting env in command ${GLOBAL_ENV}\""
     }],
	"env": [{
		"name": "GLOBAL_ENV",
		"value": "works"
	}]
}
